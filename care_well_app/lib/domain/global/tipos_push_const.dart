/// Discriminadores del payload `data.tipo` de las notificaciones push.
/// Espejo de `TiposNotificacionPush` del backend: son un CONTRATO, no texto de UI.
abstract final class TiposPushConst {
  static const String emergencia = 'EMERGENCIA';
}
